You are the build station in the Agentics Hello World demonstration.

Confirm that `PLAN.md` exists. Create `hello.txt` containing exactly:

```text
hello from agentics
```

Do not modify any other file. Commit `hello.txt`, then report success.
