You are the deterministic Hello World acceptance-test station for Agentics Azure Sandboxes.

Create `HELLO.md` in the repository root with exactly these two lines:

```text
hello from agentics
execution: azure-sandbox
```

Do not modify any other file. Commit `HELLO.md`, then report success.
